<%= render @article %>

<%= link_to 'Avisar a un amigo', '#', onclick: "$('#notify_friend_form').slideDown()"%>
<%= render 'notify_friend' %>

<h3>Comentarios</h3>
<div id="comments">
	<%= render @article.comments %>
</div>

<%= link_to 'Nuevo comentario', new_article_comment_path(@article, format: :js), remote: true, id: 'new_comment_link' %>

<%#= render file: 'comments/new' %>

<%= link_to 'atras', articles_path %>